Lo que entiendo que por cada producto hay que hacer el CRUD que seria mostrar(Show), crear(create), Listar(Index), editar (edit), eliminar(Delete).
 y hay que hacer una pagina por cada accion de cada familia.
